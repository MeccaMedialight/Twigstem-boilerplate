{# data id:sitemenu src: sitemenu.json #}
{% extends "base.twig" %}
{% block content %}
    <div class="relative overflow-hidden px-6 pb-6">
        <div class="container mx-auto relative">
            <div class="container mx-auto px-4 py-4 relative">Hey, the area of a 2m x 3m room is {{ area(2,3) }}! </div>
            {% include "_partials/hero-1.twig" %}
        </div>
    </div>
{% endblock %}