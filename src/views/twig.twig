{# data id:sitemenu src: sitemenu.json #}
{% extends "base.twig" %}
{% block content %}
    <div class="relative overflow-hidden px-6 pb-6">
        <div class="container mx-auto relative">
            <div class="container mx-auto px-4 py-4 relative">
                <p>The simplest way to extend Twig is to update the TwigExtension clas in the app directory.</p>

                <p>
                Example function call: the area of a 2m x 3m room is <b>{{ area(2,3) }}</b>!
                </p>
            </div>

        </div>
    </div>
{% endblock %}